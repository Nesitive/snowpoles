# All paths are relative to the root of the repository. Use forward slashes as separators, they will work on Windows as well.
# Traversing up is supported. Use ".." in the path to go up a directory.


[paths]
input_images = "/home/beau/Programs/SnowEx/Images/2022-2023"
images_output = "/home/beau/Programs/SnowEx/Images/2022-2023 Processed"
trainee_model = "/home/beau/Programs/SnowEx/snowpoles/models/trainee_model.pth"
trained_model = "/home/beau/Programs/SnowEx/Models/Normal/model_best.pth"
models_output = "/home/beau/Programs/SnowEx/snowpoles/models/normal"


[labeling]
pole_length = 104 # Length of poles
subset_to_label = 10 # Label every N images


[training]
lr = 0.0001 # Learning Rate 
epochs = 20 # Epochs
aug = true # Whether to augment the data
device = "cpu" # Device to use, either "cuda" or "cpu".
batch_size = 2 # Batch size
show_dataset_plot = false # Show dataset plot
filtered = 0 # Whether to apply filter to images
